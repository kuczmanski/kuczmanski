Java. Spring Boot, Security, JPA, MySQL.

Sistemas privados — o que o código faz, não um perfil:

- atendimento multi-tenant (agentes, WhatsApp, inbox, agenda, conhecimento)
- ERP (alunos, financeiro, assinatura, OAuth, webhook de pagamento)
- API de frete (Java 21) + app só como cliente
- acesso remoto (relay TLS, host, cliente JavaFX)
- gestão multi-tenant (JWT, Flyway)
- operação (tarefas, clientes, auditoria com Envers)

HTML, Node, Flutter e Python aparecem como tela, sidecar ou script. Não são o trabalho.
